@page
@model CharacterGoWModel
@{
    ViewData["Title"] = "Add Character";
}
<h1>Add Character</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div class="form-group">
                <label asp-for="Character.Title" class="control-label"></label>
                <input asp-for="Character.Title" class="form-control" />
                <span asp-validation-for="Character.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}